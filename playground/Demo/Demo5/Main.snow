module: Main
	import: stdio

    function: main
        params:
        returns: int
        body:
            declare b1: boolean = true
            loop:
                init:
                    declare i:int = 0
                cond:
                    b1
                step:
                    i = i + 1
                body:
					stdio.println(i)
                end body
            end loop
            return 0
        end body
    end function
end module