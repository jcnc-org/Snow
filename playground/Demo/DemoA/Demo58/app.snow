module: app
    import: fs
    import: fd
    import: std_io.fd
    import: fs
    import: fd
    import: std_io.fs
    import: std_io
    import: os_fs

    function: main
        params:
            declare args: any
        body:
            // 在当前目录下创建一个新目录 demoDir
            fs.mkdir("demoDir", 0)
            std_io.println("已创建目录 demoDir")

            // 在 demoDir 下再创建子目录 subDir
            fs.mkdir("demoDir/subDir", 0)
            std_io.println("已创建目录 demoDir/subDir")

            os_fs.removeDir("demoDir/subDir")
            os_fs.removeDir("demoDir")
        end body
    end function
end module
