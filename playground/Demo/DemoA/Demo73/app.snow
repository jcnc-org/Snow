module: app
    import: io
    import: process

    function: main
        params:
            declare args: any
        body:
            io.stdout_write("准备退出...\n")

            // 以退出码 42 结束进程
            //            process.exit(42)

            // 这一行不会执行
            //            io.stdout_write("这句话永远不会被看到\n")
        end body
    end function
end module
