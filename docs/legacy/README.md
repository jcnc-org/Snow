# Legacy Archive

This directory stores Java/VM-era and historical project material that is out of active Snow v1 C++ build/test scope.

Archive policy:

- Keep for historical reference only.
- Do not treat as normative for active compiler/runtime behavior.
- Do not reintroduce archived assumptions into active architecture without explicit spec updates.
